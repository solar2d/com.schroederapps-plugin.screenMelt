Sources for the plugin `plugin.screenMelt`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.screenMelt" = {
            publisherId = "com.schroederapps",
        },
    },
}
```
